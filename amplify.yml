version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd student-catalog

    build:
      commands:
        - npm start
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: /public
    files:
      - '**/*'
  cache:
    paths: []
